# 9번 과제: 자동차 vs 대중교통 의사결정 지원 웹앱

## 1. 문제 정의
많은 사용자가 자동차 구매/유지 여부를 결정할 때, 유류비만 보고 판단하거나 대중교통의 시간 손실을 정량화하지 못해 감으로 결론을 내린다.
본 프로젝트는 생활패턴 입력을 바탕으로 자차 vs 대중교통의 총비용(TCO)과 손익분기점을 계산하여 의사결정을 돕는다.

## 2. 목표(측정 가능)
- 1~10년 누적 총비용을 자차/대중교통 각각 계산
- 누적 비용 곡선의 교차(손익분기점) 존재 여부 및 시점 산출
- 시간 가치(기회비용) 포함/미포함 비교 제공

## 3. 대상 사용자
- 수도권 기준 출퇴근이 있는 직장인
- 차량 구매/유지 여부를 고민하는 사용자
- 주차비, 보험료 등 실제 고정비가 존재하는 사용자

## 4. 핵심 기능 / 부가 기능
### 핵심(MVP)
- 입력(거리/횟수/시간/비용) → 계산 → 결과(요약+표)
- 시간 가치 토글
- 손익분기점 표시

### 부가(시간 되면)
- 그래프 시각화, 시나리오 저장/비교, 유가 변동 민감도 분석

## 5. 사용자 시나리오
- 시나리오 A: 주 5회 출퇴근, 왕복 40km, 자차 60분/대중교통 90분, 월 주차비 15만 → 시간가치 포함 시 결론이 바뀌는지 확인
- 시나리오 B: 출퇴근 0~2회, 대중교통 중심 + 택시 추가비 존재 → 자차 고정비의 부담 확인

## 6. 엣지 케이스 및 처리
- 연비 0 → 계산 불가(에러 메시지)
- 음수 입력, 기간 범위(1~10) 초과 → 에러 메시지
- 시간가치 ON인데 시급 0/미입력 → 안내 메시지
- 출퇴근 횟수 0(주말만 사용)도 정상 계산 가능

## 7. 계산 규칙(요약)
- 연간 주행거리 = 왕복거리 × 주당횟수 × 52
- 자차 연간 = 유류비 + (보험/정비/세금 + 주차×12 + 통행료×12) + 감가상각((구매가-잔존가)/N)
- 대중교통 연간 = (월 교통비 + 월 추가비) × 12
- 시간가치:
  - 대중교통이 더 느리면 대중교통 비용에 시간가치 추가
  - 자차가 더 느리면 자차 비용에 시간가치 추가

## 8. 개선 과정 기록(예시)
- v0: 입력 폼/기본 계산
- v1: 검증(엣지케이스) 추가
- v2: 손익분기점 로직 추가
- v3: 문서화/README 정리

## 9. AI 활용 기록(예시)
- AI에게 제공한 컨텍스트: 입력 항목, 단순 감가모델, 손익분기점 정의, 엣지케이스 목록
- 검증: 연비 0, 음수, 기간 범위, 시간가치 토글에 따른 결과 변화 확인
- 수정: 시간가치 적용 규칙을 일관되게 문서화하고 UI에도 표시하도록 개선
